/* js/build_edit/build_edit.js.txt */

function save(event){
  const item = event.currentTarget;
  const name = item.getAttribute("name") || "";
  if(name === "activities" || name === "macros" || name === "displays" || name === "modules"){
    execute("write_"+name,db[name])
	return;
  }
  console.log()
}

function build_edit(db,item) {
  console.log("build_edit")
  console.log(item);
  switch(item)
  {
    case "Activities":
	  return build_activities(db);
	break;
    case "Macros":
	  return build_macros(db);
	break;
    case "Displays":
	  return build_displays(db);
	break;
    case "Modules":
	  return build_modules(db);
	break;
    default:
	  console.log("Unkown edit "+item)
  }
  let dom=document.createElement("div");
  dom.innerHTML=JSON.stringify(item)
  return dom;
}
