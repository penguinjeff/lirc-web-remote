/* js/build_remote/build_button.js.txt */

function build_button(remote = "", button_name = "", button_display = "", title = button_display) {
    function decodeHtmlEntities(str) {
        const tempElement = document.createElement('div');
        tempElement.innerHTML = str;
        return tempElement.textContent || tempElement.innerText;
    }

    if (button_name === "") {
        const dom = document.createElement('div');
        dom.className = "empty";
        return dom;
    }

    // Build inline event strings
    const common = "(event)";
    const presshold_str = "presshold" + common;
    const release_str   = "release"   + common;

    const dom = document.createElement('button');
    dom.className = "button";
    dom.setAttribute("name", remote + " " + button_name);

    // Tooltip
    dom.title = decodeHtmlEntities(title);

    // Inline event attributes (string form)
    dom.setAttribute("onpointerdown", presshold_str);
    dom.setAttribute("onpointerup",   release_str);
    dom.setAttribute("onpointerleave", release_str);

    // Visible label
    dom.innerHTML = button_display;

    return dom;
}
